<!-- each message is rendered with this template -->

<!-- align left or right if its self or other -->
<!-- class is set to self or other -->
<li class="other<%#=  self_or_other(message) %>">
  <div class="avatar">
    <img src="http://placehold.it/50x50" />
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).email %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>